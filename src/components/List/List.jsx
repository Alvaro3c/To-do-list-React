import React, { useState } from "react";
import data from './data'

const List = () => {
  /*   const [tasks, setTasks] = useState(data)
    setTasks(...tasks) */

  return <form action="">
    <h1>To do list</h1>
    <input type="text" />
    <button>ADD</button>
  </form>
};

export default List;
